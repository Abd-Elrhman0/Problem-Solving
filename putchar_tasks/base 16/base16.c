#include <stdio.h>
#include <string.h>

int main(void)
{
        int x;
       for (x = 0x30; x <= 0x39; x++)
        {       putchar(x);
}
for (x = 0x61; x <= 0x66; x++)
        {       putchar(x);
}

printf("\n");
return 0;
}
