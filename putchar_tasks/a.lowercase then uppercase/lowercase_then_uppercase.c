#include <stdio.h>
#include <string.h>

int main(void)
{
        int x;
        for (x = 0x61; x <= 0x7A; x++)
        {       putchar(x);
}
for (x = 0x41; x <= 0x5A; x++)
        {       putchar(x);
}

printf("\n");
return 0;
}
